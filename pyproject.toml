[project]
name = "historical-smell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bs4>=0.0.2",
    "datasets>=4.5.0",
    "flair>=0.12",
    "numpy>=2.4.0",
    "transformers>=4.57.6",
    "evaluate"
]
